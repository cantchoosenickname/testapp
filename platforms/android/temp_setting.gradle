android {
            defaultConfig {
                applicationId = "org.nativescript.testapp"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }